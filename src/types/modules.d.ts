// Ambient declarations for third-party modules that ship no TypeScript types.
// They resolve as `any` so the app type-checks; behaviour is unchanged.
declare module 'react-native-circular-progress';
declare module 'react-native-event-listeners';
