<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
      LAN-only tool: the app's only network activity is a cleartext WebSocket
      (ws://) to the bridge server on the local network. Android blocks cleartext
      by default in release builds — the React Native Gradle plugin sets
      usesCleartextTraffic=false for `release` (see AgpConfiguratorUtils.kt) — which
      silently stops the CI release APK from connecting at all (the WebSocket fails
      inside the app before any packet leaves the phone). We force it true so the
      app can reach the LAN bridge. Safe because the traffic never leaves the local
      network; never point this app at a public host.
    -->
    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="true"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
